# AUTO-GENERATED - do not edit manually.
# Source: shared/config.json

ARENA_WIDTH = 80
ARENA_HEIGHT = 60
PADDLE_WIDTH = 2
PADDLE_HEIGHT = 12
PADDLE_SPEED = 2
BALL_SIZE = 2
BALL_INITIAL_SPEED = 0.15
BALL_MAX_SPEED = 6.0
BALL_ACCELERATION_FACTOR = 1.05
SCORE_TO_WIN = 21
FPS = 60
BITMAP_WIDTH = 80
BITMAP_HEIGHT = 60
FRAME_STACK = 5
FRAME_STEP_TICKS = 5
INFERENCE_INTERVAL_TICKS = 30
HEURISTIC_INTERVAL_TICKS = 60
DEFAULT_SEED = 42

CONFIG = {
    "arena_width": ARENA_WIDTH,
    "arena_height": ARENA_HEIGHT,
    "paddle_width": PADDLE_WIDTH,
    "paddle_height": PADDLE_HEIGHT,
    "paddle_speed": PADDLE_SPEED,
    "ball_size": BALL_SIZE,
    "ball_initial_speed": BALL_INITIAL_SPEED,
    "ball_max_speed": BALL_MAX_SPEED,
    "ball_acceleration_factor": BALL_ACCELERATION_FACTOR,
    "score_to_win": SCORE_TO_WIN,
    "fps": FPS,
    "bitmap_width": BITMAP_WIDTH,
    "bitmap_height": BITMAP_HEIGHT,
    "frame_stack": FRAME_STACK,
    "frame_step_ticks": FRAME_STEP_TICKS,
    "inference_interval_ticks": INFERENCE_INTERVAL_TICKS,
    "heuristic_interval_ticks": HEURISTIC_INTERVAL_TICKS,
    "default_seed": DEFAULT_SEED,
}
